id: "X-2"
question: "Q-01"
why_now: "campaign 每轮末由 orchestrator 机械跑一次 32B 泛化验证,数字只在 digest 呈给人;round-subagent 与 Critic 禁读。25 行聚合结果从未被任何节点解释。"
modifies_artifact: false
inputs:
  - ref: "X-1/result#detector"
    use: "每轮的候选包(按 validation 行的 artifact_sha 对应到该轮 attempt)"
  - ref: "X-1/result#ledger"
    use: "ref 字段取该轮的 R-id"
plan: "每轮:python3 judge/validate_32b.py --ref R-NNN —— 对 Qwen3-32B 集 4864 条(615 正 / 4249 负,GT ffee0a5c3ddc)以同一 token-only 接口检测,只输出一行聚合 JSON 追加到 runs/validation.jsonl;不落盘任何逐条结果。"
status: "closed"
result:
  findings:
    - id: "finding-1"
      text: "R-025 在 32B:val_tpr 0.443902(273/615),val_fpr 0.008708(37/4249)"
      conditions: "32B 集 4864 条(v1 clean/clean2/fault、v2 clean/fault;v2 fault 仅 768 条不完整;无 v2-hard);GT 为独立标注 ffee0a5c3ddc,含 think 段异常;与 8B 同轮分数(0.706)不可直接比,两集正例构成未对齐"
      evidence:
        - "X-2/val:R-025"
        - "X-1/R-025"
    - id: "finding-2"
      text: "32B 误报按轮次增量(每轮单因子):R-001 起 1 条,R-002(exact_loop)+1 又被 R-003 的 min_copies 守卫收回;R-010(splice 两种容错)+10;R-011(foreign_island)+3;R-015(cjk_island)+6;R-016(cjk_island 贴数字)+1;R-017(cjk_splice_single)+2;R-020(cjk_splice_single_space)+17;R-021(cjk_splice_single_ws)+7;R-024(max_foreign=60)−10;其余轮 0;HEAD 合计 37。repetition 族(R-008/009/014/022/023)在 32B 上 0 新增误报"
      conditions: "增量归因依赖『每轮只改一个因子』且规则优先级固定;它只反映加入当时的边际误报,不等于在 HEAD 上删掉该规则会减少的误报"
      evidence:
        - "X-2/val:R-001"
        - "X-2/val:R-002"
        - "X-2/val:R-003"
        - "X-2/val:R-009"
        - "X-2/val:R-010"
        - "X-2/val:R-011"
        - "X-2/val:R-015"
        - "X-2/val:R-016"
        - "X-2/val:R-017"
        - "X-2/val:R-020"
        - "X-2/val:R-021"
        - "X-2/val:R-023"
        - "X-2/val:R-024"
        - "X-2/val:R-025"
    - id: "finding-3"
      text: "R-025 的 cjk_splice_pair_space 在 8B 为空桶,但在 32B 新增 10 条检出(263→273),误报不变(37)"
      conditions: "单因子轮;聚合数,未核对这 10 条属于 GT 的哪一类异常"
      evidence:
        - "X-2/val:R-024"
        - "X-2/val:R-025"
        - "X-1/R-025"
    - id: "finding-4"
      text: "R-024 的 max_foreign=60 在 32B 减少 10 条误报(47→37),减少 1 条检出(264→263)"
      conditions: "单因子轮;8B 上零行为变化(X-1/finding-5)"
      evidence:
        - "X-2/val:R-023"
        - "X-2/val:R-024"
  products:
    - id: "validation-ledger"
      path: "campaign/runs/validation.jsonl"
      version: "0beb19bf2b8d"
      interface: "每行一个 validate 事件:t、ref、artifact_sha、val_tpr、val_fpr、n_detected/n_should、n_false_positive/n_should_not、n_skipped、n_records、gt_sha12、gt_pinned;25 行 ref=R-001..R-025"
      status: "usable"
      gaps:
        - "被 .gitignore,不在 git 历史中,只有盘上副本(版本 = 文件 sha12)"
        - "无规则级、无逐条信息(设计如此)"
    - id: "validate-tool"
      path: "campaign/judge/validate_32b.py"
      version: "5bc725ea099a"
      interface: "python3 judge/validate_32b.py --ref <标签> --candidate <包目录>;聚合 only;追加到 cwd 下的 runs/validation.jsonl;本机一次约 136 s"
      status: "usable"
      gaps:
        - "--ref 可填任意字符串,与 recorder 账本无联动,只能靠 artifact_sha 对上候选包"
        - "首次加载 tokenizer 可能访问 HF Hub(本机有缓存;离线设 HF_HUB_OFFLINE=1)"
  runs: []
  limitations:
    - "关闭原因:随 X-1 停止而停止;每轮一行、无漏跑;本节点没有 recorder run,证据全是 validate 行"
    - "只有聚合数,不能区分 32B 误报是规则问题还是 32B GT 漏标"
    - "0817 六格 campaign 的误报预算 FPR ≤ 0.1% 对应本集 ≤ 4/4249,当前 37"
    - "本地 32B 集与 0817 campaign 的 wb32b 格不同(后者含 v2-hard 文件,GT 版本未知),结论迁移有条件"
    - "campaign 规则禁止 round-subagent/Critic 读 32B 目录与本账本;后续节点读本账本、跑 validate 属新授权(本试用的默认授权:允许聚合、禁止逐条;待人确认)"
  next:
    - "按规则逐条消融(改候选包 config 的 enable / 阈值端点)重跑 validate,做 HEAD 上的规则级归因"
    - "找 8B 不掉 TP 且 32B FP ≤ 4 的子集;若不存在,给出 TP/FP 权衡表"
