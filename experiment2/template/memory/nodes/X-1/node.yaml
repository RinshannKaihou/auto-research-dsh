id: "X-1"
question: "Q-01"
why_now: "noProb 线(检测输入只有 token_ids)从全关基线起,在 8B 评分集上以 FPR=0 硬门逐轮单因子演化检测规则;前一条 logprob 线(E-009)的结论只作起点参考。2026-08-13 人工 steer 指定 Round A/B 后封口。"
modifies_artifact: true
inputs: []
plan: "每轮一个因子:离线精确重放当前 champion(零差)→ 改 config/代码 → recorder 进账 8B 分数(gate: FPR=0)→ orchestrator 跑一次 32B 聚合验证(见 X-2)→ 写 round 文件与 STATE;每 lease 末 Critic 机械审计。R-024 起按人 steer:Round A = foreign_splice 加全序列外语 token 总量上限 K;Round B = 恰 2 个互不相邻 CJK、双侧带空格词干。"
status: "closed"
result:
  findings:
    - id: "finding-1"
      text: "token-only 检测器在 8B 的 0-FP 前沿为 636/901(TPR 0.705882),R-023 达到;R-024(max_foreign=60)与 R-025(cjk_splice_pair_space)在 8B 上判定集不变(三者 sample_sha 同为 3a87f3ed8d41)"
      conditions: "8B 评分集 6912 条、GT v2 901 正例(400493ad4655)、negative_control 为 FPR=0 硬门"
      evidence:
        - "X-1/R-023"
        - "X-1/R-024"
        - "X-1/R-025"
    - id: "finding-2"
      text: "HEAD 启用 17 条规则,分两族:rare-character 11 条(foreign_splice 及其容错/总量上限/泰文 presence/外语孤岛、fffd_embed、feff_embed、hebrew_island、glyph_repeat、cjk_island、cjk_splice_single 的四个邻接变体、cjk_splice_pair_space)与 repetition 6 条(exact_loop、block_repeat、packed_repeat、block_repeat_large、template_enum、sentence_repeat);加入顺序与 R-id 对应,见 config.yaml 注释"
      conditions: "detector() 按固定优先级短路:先 rare-character 各条,后 repetition 各条;一条命中即返回,不报规则名"
      evidence:
        - "X-1/R-001"
        - "X-1/R-002"
        - "X-1/R-007"
        - "X-1/R-008"
        - "X-1/R-009"
        - "X-1/R-010"
        - "X-1/R-011"
        - "X-1/R-012"
        - "X-1/R-013"
        - "X-1/R-014"
        - "X-1/R-015"
        - "X-1/R-016"
        - "X-1/R-017"
        - "X-1/R-018"
        - "X-1/R-019"
        - "X-1/R-020"
        - "X-1/R-021"
        - "X-1/R-022"
        - "X-1/R-023"
        - "X-1/R-024"
        - "X-1/R-025"
    - id: "finding-3"
      text: "三条 repetition 规则的阈值与 8B clean 边界只差 1:block_repeat 11 次(clean 最高 10)、template_enum 6 条(clean 最高 5)、sentence_repeat 总 5 次且 800 字符内 4 次(clean 最高 4)"
      conditions: "8B clean 6011 条;campaign 内已标为 32B val_fpr 观察点,未在 32B 上核实"
      evidence:
        - "X-1/R-008"
        - "X-1/R-022"
        - "X-1/R-023"
    - id: "finding-4"
      text: "R-025 的 cjk_splice_pair_space 在 8B 全库为空桶(0 TP / 0 FP):全库唯一『恰 2 个互不相邻 CJK』的记录 v1-wb363 是合法日文混排,邻接类别不进格"
      conditions: "人 steer Round B,按指定实现并提交;是否在别的模型上非空未知"
      evidence:
        - "X-1/R-025"
    - id: "finding-5"
      text: "R-024 的 max_foreign=60 在 8B 上零行为变化:86 条 splice TP 中 85 条 n_foreign=1,唯一例外 v2-wb579(57);should_not 侧 56 条 n_foreign>0 的记录本无 splice 命中"
      conditions: "人 steer Round A;目的是结构性排除『整段换语言』形态,效果只能在 8B 之外看"
      evidence:
        - "X-1/R-024"
  products:
    - id: "detector"
      path: "campaign/monitor/msprobe_response_anomaly"
      version: "f087bd175436"
      interface: "ILLDetector(configs/config.yaml, configs/mtype_config.json, token2category/).detector(token_ids, model_name) → DetectionResult(is_ill, ill_type);输入只有 token_ids + 静态 model_name('qwen3');17 条规则由 config.yaml 的 enable/阈值控制(全关 = 取永不触发端点);解码经本机 HF 缓存的 Qwen/Qwen3-8B tokenizer。版本 = recorder 目录指纹(R-025 attempt 的 artifact_sha;git 8bfb3a8 起未变,HEAD 0c902d7)"
      status: "usable"
      gaps:
        - "不输出命中的规则名,规则级归因要靠改 config 消融或改代码"
        - "全部阈值只在 8B 定档"
        - "整链 8B 评分约 220–273 s 一次(sentence_repeat 解码开销)"
    - id: "r025-sample"
      path: "campaign/runs/artifacts/R-025/sample"
      version: "3a87f3ed8d41"
      interface: "目录下 results.json = {文件名: [{seq_id, is_ill, ill_type, n_tokens}]},9 个文件 6912 条,636 条 is_ill;版本 = recorder 的 sample_sha(对 sample/ 目录的指纹),与 R-023/R-024 相同"
      status: "usable"
      gaps:
        - "只有 8B;无规则名"
        - "runs/artifacts/ 被 .gitignore,只存在于盘上"
    - id: "ledger"
      path: "campaign/runs/events.jsonl"
      version: "ed06295c975e"
      interface: "recorder 单写;attempt / result / replay / epoch 事件各一行;读法 python3 tools/run_record.py recent | digest | replay --id R-0NN"
      status: "usable"
      gaps:
        - "digest 的 champion 同分取最早(R-023),与 STATE.md 的写法不一致"
    - id: "history"
      path: "campaign/STATE.md"
      version: "727c0fdcbff6"
      interface: "STATE.md 498 行(冠军表 / 累积教训 / 未决问题);campaign/memory/rounds/round-001..025.md 每轮的假设-探查-实施-结果-教训;campaign/memory/audit/after-round-020|025.md 为 Critic 审计;git 历史至 0c902d7"
      status: "usable"
      gaps:
        - "STATE.md 未压缩,且冠军表把 R-025 写成 champion(recorder 为 R-023)"
        - "各轮提到的『离线 judge-loop 精确重放』脚本不在仓库内(仓库没有 scratch/),已丢失"
  runs:
    - "R-001"
    - "R-002"
    - "R-003"
    - "R-004"
    - "R-005"
    - "R-006"
    - "R-007"
    - "R-008"
    - "R-009"
    - "R-010"
    - "R-011"
    - "R-012"
    - "R-013"
    - "R-014"
    - "R-015"
    - "R-016"
    - "R-017"
    - "R-018"
    - "R-019"
    - "R-020"
    - "R-021"
    - "R-022"
    - "R-023"
    - "R-024"
    - "R-025"
  limitations:
    - "关闭原因:人 steer 的 Round A/B 封口后按指令停止(『停,呈报人审阅,不自行发明下一候选』);不表示 8B 前沿穷尽或 32B 问题解决"
    - "全部阈值只在 8B 定档;32B 只做过聚合验证(见 X-2),没有规则级归因"
    - "recorder 的 champion 为 R-023(同分取最早),STATE.md 与 round-024/025 写成 R-024/R-025;三者 8B 判定集相同,产物版本以 artifact_sha 为准"
    - "各轮的离线 judge-loop 精确重放脚本未保存;要逐条模拟需重建(推荐直接用候选包 + recorder / validate)"
    - "检测器不输出命中规则名"
    - "GT 双胞胎冲突使若干形态不可用(FEFF 词间带空格 v1-wb31/594、数字间 v2-wb308/379、U+200C v2-wb433 等),见 STATE 累积教训"
    - "STATE『未决问题』列出的 8B 剩余漏检(约 104 条文本级无信号、约 97 条 soft repetition、混排歧义区)已判为 8B 不可分,未在 32B 上看过"
  next:
    - "Q-01:对 R-025 检测器做规则级 32B 误报归因(聚合 only),给出 8B 不掉 TP 前提下 32B FP ≤ 4/4249 的规则子集,作为 0817 六格 campaign 起始候选的依据"
    - "Q-02:核对 token_traj_detector 端口与 R-025 在 8B 上是否逐条等价(见 X-3)"
