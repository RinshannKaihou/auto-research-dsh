# auto-research v5：v0.1 实施计划

2026-09-13：核心运行流程与交互式研究路径图均已实现并完成工程验收。按用户最新要求，地图属于 v0.1 必交范围。下面保留实施基线并同步这一范围变化；实际结果与平台边界见 [docs/VALIDATION.md](docs/VALIDATION.md)。

目标代码目录：`/Users/ywang2397/work/agent-research/auto-research-init-v5`。
旧 v4.2、旧 v5 原型及 Fable 的实验材料保留，不在其上改造运行中的研究项目。

## 1. 这一版要交付什么

交付一个能实际运行的研究框架：给定研究目标和可用材料，协调者可以提出探索、并行派发、保存阶段性工作、组合多条分支，并在停止或重启后继续。节点可以没有实验、没有发现，甚至没有完成原计划；必须交代实际进展、材料位置和未决事项。

“完整”指这条流程完整，不指功能齐全，也不指已经证明它比 v4.2 更会做科研。
不再以找到合适的研究题目、或证明字段化历史优于自然语言历史，作为开发前置条件。

支持理论推导、反例搜索、假设比较、方法设计、跨阶段综合以及代码实验。初始化不要求评价器、分数、冠军产物或可预先写出的正确答案。

## 2. 最小研究协议

### 节点是一段工作，不是一次结论验收

一个问题可以跨越很多节点；一个节点可以引用多个问题相关的历史结果，选择一个主要问题说明本次目的。节点边界由本次投入范围与交接点决定，不要求每个节点回答一个完整问题。

| 信息 | 内容与约束 |
|---|---|
| 身份与目的 | `id`、议程中的 `question`、`why_now`、本次 `plan` 与投入边界 |
| 输入 | `inputs`：固定材料的 `ref` 和使用理由 `use`；支持多个来源 |
| 权限 | 可读材料、私有写入目录、是否允许修改指定目标产物；权限由运行环境兑现 |
| 进展 | 工作期间保存简短进度、当前文件、未决事项；重启后可以接手 |
| 产物 | `products`：稳定编号、归档位置、程序计算的版本、用途/接口、已完成部分与缺口 |
| 发现 | 可为空；每项有正文、适用条件和 `evidence`；可选 `revises` 指向先前发现 |
| 交接 | 独立的 `close_reason`、`limitations`、建议的 `next`；不把关闭原因藏在正文首行 |

程序只检查引用是否存在、内容版本是否吻合、权限和生命周期是否符合约定。引用存在不表示证据足够，节点关闭不表示结论成立。

证据可以是实验记录、数据片段、归档文献摘录、推导文件、证明检查输出或其他已固定产物。推导是否正确、条件是否充分仍由研究者判断。尚未成立的猜想和缺口可以直接保存在计划、笔记和产物中，不必伪装成 finding。

`revises` 表示某项发现有后续修订；允许修订解释，也允许纠正计算、数据或测量错误导致的观察性结论。保留旧记录及修订理由，不自动判废，不自动撤销整个节点或它的后继节点。

### 图的关系与执行等待分开

- 第一版只把已关闭节点发布的固定材料作为跨节点输入；关闭可以发布半成品。
- 两条分支可以同时运行；一条完成后就可继续衍生，不等整批结束。
- 汇聚是引用多个分支的普通节点，无专门的“合并成功”状态，也不自动合并 Git 代码或科学结论。
- 回到旧问题时创建新节点并引用有关材料。研究可以反复回访；按发布时间组织的材料来源关系自然不会回指尚未产生的结果。
- 不给整个研究强加 DAG 要求，也不让每条“相关”关系自动变成调度依赖。v0.1 不实现运行中节点互相引用可变文件。

节点只需要 `proposed / open / closed`。执行尝试另外记录排队、运行、完成、中断和状态待核实；一次工具或模型故障不直接变成科学负结果。已关闭节点不重新打开，续写工作另建节点；未关闭节点的恢复可以换新 agent，不依赖原会话。

## 3. 程序怎样支持研究

### 一份权威状态，一份可读材料库

采用 Python + SQLite 管理节点、引用、执行尝试、预算预占、议程版本和操作事件；文件保存报告、推导、代码、日志及输入输出快照。SQLite 是元数据权威源，不能声称删库后靠报告完整重建。

节点卡片和索引由数据库导出，agent 提交文件经程序校验后导入。它们不是第二份可任意修改的权威账本。议程和研究笔记允许人及协调者修改，但每次修改保留版本，本次派发记录实际采用的版本。

固定材料使用完整内容摘要。文件与目录各有明确计算规则，版本由程序生成，不要求 agent 手填。外部大数据只登记来源、可用性和校验信息；没有复制的数据不能宣称离线可复现。

发布顺序为：准备文件快照 → 校验与固定 → 数据库事务登记引用、结果与关闭状态。崩溃可以留下未引用文件，不能留下指向不存在材料的已发布节点。备份同时包含数据库的一致性备份及它引用的文件，视图可以重新生成。

### 一个协调者，少量并行执行者

第一版只支持单机、一个协调者，默认同时运行两个探索节点，并发数可配置。

协调者读取目标、当前议程、开放问题、正在执行的节点和相关历史，提出下一步或调整待执行提案。程序验证提案、领取节点、预占预算，再派给执行者。执行者提交进展、材料、发现和后续建议，程序校验发布，协调者据此决定是否续写、另开分支或汇聚。

选择研究方向是 agent 的工作；程序不做“最高科学价值”评分。没有可派发任务时说明是在等待、预算不足，还是尚无下一步建议，不能自动把课题判为已解决。

每次上下文至少包含目标和约束、选中的问题、直接输入与用途、相关修订、未决事项和预算。另提供全局索引及按节点/引用查询历史的工具；不只喂上一轮摘要，也不把全历史强塞进每次上下文。计划外实际使用了新的历史材料，应在交接时补充对应引用。

### 隔离与运行后端

研究核心不绑定本次开发所用的 Codex 会话。优先接已有 DSH 环境，以薄适配层调用核心；只接一个实际后端，不同时做 Codex、Claude Code、多供应商和远端调度。

DSH 是否满足新版本要求，在 M0 用小型运行核实：启动、输出采集、身份记录、终止、权限及重新连接/判定结束。依赖能被导入不算验收通过。不能实现的控制必须明确记录，不能把 prompt 里的要求写成“已经强制”。

当前已发现的具体适配问题：安装的 DSH 子代理接口从父会话继承工作目录，没有逐次 start 的 cwd 参数；旧原型与当前 profile 还分别解析到不同版本依赖。因此 M0 必须验证两个同时存活的 worker 的真实工具工作目录彼此独立。优先用节点级会话或现有 provider 能力解决，不用提示词假装已经进入 worktree。这是后端适配问题，M1 的存储实现可以并行推进。

每次执行独占工作目录；在现有 Git 代码上改动时使用 worktree，纯文档/推导任务使用私有目录和输入快照。共享历史材料固定且不可原地改写，续写使用自己的副本。`modifies_artifact: false` 仍允许写私有探针、推导和报告，不等于整个工作目录禁写。

先用真实运行环境验证保护规则；哈希差异检查只用于发现改动，不能冒充访问隔离。Worker 不直接改数据库，变更经绑定身份和作用范围的程序入口提交。

### 预算与恢复

- 领取任务与预算预占放在同一事务中；节点、协调者及其他模型调用都计入开销。
- 并发数、受管调用次数和受管进程超时可以由本地程序限制；后端无法硬限制的 token/费用预算只能作为派发门槛与估算，不承诺绝不超支。
- 实际用量、估算与未知分开保存。缺失用量不能记为零；超出估计照实记账并阻止继续透支，未知执行保留占用直到核实。
- 派发前持久化唯一执行编号，记录后端会话/作业标识及进程信息。程序重启先核实旧执行，不能看到 open 就重发或全部作废。
- 能确认存活则继续收集；能确认停止则保存工作现场并安排接手；不能确认则标为待核实，不自动重复执行。恢复不要求续用同一段模型对话。
- `pause` 停止新派发并让当前工作收尾；显式终止会保存现场并停止受管作业。两种行为在 CLI 和状态视图中分清。收到取消请求的回执不等于执行已结束，确认终止并结算后才能释放占用。

## 4. 实现顺序和完成标准

里程碑是同一 v0.1 的施工顺序，不在每一步重新寻找研究题目或开展效果对照实验。

| 阶段 | 实现内容 | 完成标准 |
|---|---|---|
| M0：核心契约与后端核实 | 独立短协议、目录和 CLI 约定；最小 DSH 适配验证；明确可硬控/只能估算的能力 | 两个小 worker 的真实工具目录互相隔离；能采集结果并确认终止；受限写入有实际证据。若 DSH 不满足，先解决适配阻碍，不把问题埋进核心 |
| M1：持久化研究状态 | SQLite、初始化、议程版本、节点生命周期、引用、半成品发布与查询 | 不配置评价器也能完成“零实验节点发布半成品 → 新节点引用续写”；错误引用拒绝，已发布版本保持固定 |
| M2：执行与恢复 | 私有工作区、执行尝试、原子领取、预算、进度保存、暂停/终止/恢复 | 两个真实子进程不会抢到同一节点；重启不会重复派发、覆盖历史或丢失预算占用；崩溃现场可接手 |
| M3：研究协调循环 | 协调者和执行者指令、历史检索、提案校验、并行分支、多输入节点、主动记录修订 | 真实 agent 经同一条执行链读取图、产出新节点并提出后续；下一步来自已保存状态，不依赖本对话补充上下文 |
| M4：用户操作与交付 | 安装入口、`init/start/status/show/pause/resume/stop/steer/check`、导出/备份、使用说明 | 用户可在新目录从零启动、查看谁在做什么及为什么、调整议程和预算、暂停后重新打开继续 |
| M4.1：交互式研究路径 | `ari view` 本地实时页面、离线 HTML 导出、方向图、节点详情、引用跳转与上下游追踪 | 实际研究状态驱动图形；能看分叉与汇聚、半成品和修订，缩放拖动筛选；关闭节点不显示为问题已解决；浏览器实际交互通过 |
| M4.2：GUI 操作入口 | 双击启动、新建课题表单、本机材料选择、已有课题打开、起停与设置、内嵌研究图 | 用户不填写命令即可从空项目进入研究；创建不调用模型，开始按钮调用同一Runtime；重复点击不重派，控制操作与状态更新经过真实浏览器验证 |
| M5：整体验收 | 完整流程测试、关键故障注入、真实后端烟测；修复阻断问题 | 下述端到端验收全部通过，交付可用 v0.1；再进入真实复杂研究试用 |

拟定目录：`src/auto_research/` 放核心，`apps/dsh/` 放首个后端适配，`tests/` 放测试，`docs/` 放协议和使用说明，`examples/` 放完整流程示例。运行数据与框架源码分开。

## 5. 验收一条完整流程

选择可在本机低成本执行的确定性材料，只检验系统能否按约工作，不冒充研究效果实验：

1. 从空目录初始化课题，不提供 evaluator；A 发布一个有未证条件的推导草稿，零 run、空 findings。
2. B 继续推导，C 搜索反例，两者分别读取 A 的固定材料，在独立目录实际并行运行。
3. B 交出未完成工作和一项附证据、保留条件的局部 finding，D 可先接手 B；C 尚未结束时不阻塞这条分支。
4. C 读取 B 已发布的 finding，发现其前提需要修订，保留旧记录、反例证据及修订关系；E 读取 C、D 的不同版本材料，做一次多输入综合，允许仍然没有最终答案。对 A 草稿的质疑通过产物引用说明，不把 `revises` 指向不存在的 finding。
5. 在运行中暂停或杀掉协调进程，重新启动。验证旧作业被核实、未重复派发、费用没有归零；新的 agent 仅靠持久化内容接手。
6. 修改议程、耗尽预算、重复提交结果，再检查状态、引用和备份恢复后的材料是否一致。

对领取竞争、发布中断、退出但结果未入账、用量缺失等关键边界做故障注入。测试验证可观察行为和持久化结果，不只是让测试脚本复述实现。关键服务覆盖率按上级 AGENTS.md 要求达到 90% 以上，同时保留真实进程和文件系统验收。

确定性流程通过后，用少量真实 agent 运行同一套入口，核实上下文、提交格式、后端控制和新会话接手。不会要求模型恰好在某个预算点做不完：工程测试可主动中断，真实研究中的半成品边界留待试用观察。

科学收益在 v0.1 之后另验：长期研究能否减少遗忘与重复劳动、保留前提和分歧、提高综合质量。此次验收不声称回答这些问题。

## 6. 旧代码怎样处理

- **保留并借鉴**：证据存档、内容摘要、命令输入校验、实际/估算成本区分、权限范围、测试中已经发现的失败案例。
- **经接口改造后再复用**：执行记录与结果采集、DSH 子代理调用及工具日志。新旧语义不一致的部分不直接复制。
- **重写**：单任务串行状态机、把所有 open 判为 stale 的恢复逻辑、旧契约/主张审批流程、自动 best/champion 与固定 evaluator 假设。
- **实验材料只作样例**：保留 experiment1 的部分产物与引用回归案例、experiment2 的历史材料引用示例；不沿用其文本/结构效果评分作为框架验收。

## 7. 此版明确不做

多机、大规模 agent 群、自动科学真伪裁决、独立性计算、复杂 Claim 生命周期、专属 merge/revisit 状态机、MCTS/收益预测调度、网页直接改写已归档结论、强制每批 Critic、自动迁移所有旧研究。

有需要时，复核就是普通探索，可以由另一个 agent 执行。结构化字段保持少量；只有程序确实需要不同处理时才新增字段或类别。

## 8. 资料地位

本计划按用户“先实现完整 v0.1，再测试”的最新方向制定。详细审阅见 [BRAINSTORM_REVIEW.md](/Users/ywang2397/work/agent-research/auto-research-init-v5/BRAINSTORM_REVIEW.md)。原长文保留为设计历史，不作为运行时提示词或需要逐节合并的执行规范。实施中如发现必须改变的基础约定，集中更新短协议及本计划，不继续在长文末尾追加新版本。
